public class Client {
    SecurityCredentials credentials;
    public Client() {
        credentials = new SecurityCredentials("admin");
        //this can represent any user with any trust level, i am just using this for placeholder purposes
    }
}
